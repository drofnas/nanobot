services:
  tailscale-helper:
    image: tailscale/tailscale:latest
    container_name: nanobot-helper-tailscale
    hostname: nanobot-helper
    environment:
      # One-time auth on first boot; state persists across restarts.
      # Provide at runtime: NANOBOT_HELPER_TS_AUTHKEY="tskey-..." docker compose ... up -d
      - TS_AUTHKEY=${NANOBOT_HELPER_TS_AUTHKEY:-}
      - TS_AUTH_ONCE=true
      - TS_STATE_DIR=/var/lib/tailscale
      - TS_USERSPACE=false
      # Optional hardening/UX knobs:
      # - TS_ACCEPT_DNS=true
      # - TS_EXTRA_ARGS=--advertise-tags=tag:container
      - TS_HOSTNAME=${NANOBOT_HELPER_TS_HOSTNAME:-nanobot-helper}
    volumes:
      - nanobot-helper-tailscale-state:/var/lib/tailscale
    devices:
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - NET_ADMIN
    restart: unless-stopped

  nanobot-helper-gateway:
    network_mode: service:tailscale-helper
    depends_on:
      - tailscale-helper
    # Tailscale-only exposure: the gateway port is reachable via the tailnet,
    # but not published on the Docker host.
    ports: !reset []

volumes:
  nanobot-helper-tailscale-state:
    driver: local
